# Solver v0.4 (O1+O6 + LBBD cut loop) — Full Sweep Panel

Run date: 2026-07-25 (evening), on commit `94cee2d` ("Wire the LBBD cut loop"),
tag base `v0.3-O1O6` + LBBD. Run by Claude (queue item 2; the standing eva
protocol was followed: one subprocess per instance, sequential, hard external
timeout, verdict by `utils.check_feasibility` on the returned solution only,
never the solver's own claim). Env: `ogc2026` conda env, `timelimit=60`.

CSV: `results/2026-07-25_solver_v0.4_lbbd_full_sweep.csv`
Raw JSONL: session scratch `sweep_lbbd.jsonl` (39 rows).

Provenance caveats, stated up front:
* The sweep process was externally stopped after prob_39; the **prob_40 row
  comes from a single same-protocol run ~50 min earlier** on the same commit
  (noted in the CSV). Every other row is from the sweep proper.
* **N=1 per instance.** Per the F-log's timing-nondeterminism finding
  (2026-07-25 §F10 note: prob_26 ranged 23.4M–27.3M at the same timelimit),
  overloaded-instance objectives here are indicative, not reproducible;
  ship/no-ship decisions still need N≥5 reps.

## Headline

* **40/40 feasible, 0 × −1.** Worst wall 52.03 s (prob_21) against the
  54.8 s internal budget; the v0-table hard timeouts on prob_38/40 are gone.
* vs the pre-O1 v0 table (`2026-07-25_solver_v0_full_sweep.csv`):
  **36 improved, 2 fixed from −1 (prob_38/40), 2 exact ties (prob_2/8), 0
  regressions.** Median improvement on changed instances ≈ −80%. That delta
  bundles everything landed since (O1+O6 exact master, deadline discipline,
  repair/polish, LBBD); it is a baseline refresh, not an LBBD-only A/B.
* **LBBD-specific delta** (vs the F8 table's best-z1=0 on the same build,
  same day): prob_3 82,760 → 52,060 (−37%), prob_4 50,332 → **16,916,
  proven optimal** (`master_bound_closed`), prob_5 81,407 → 74,937 (−8%),
  prob_14 ~300,481 → 228,876 (−24%); prob_6/7 walked 19/15 candidates with
  no improvement (prob_7 landed 2 certified θ cuts). prob_1/2/8 now stop at
  their certified assignment-layer optimum in < 3 s instead of idling.

## Per-tier notes

* Easy tier (prob_1–8): z1 = 0 everywhere; three instances close at a
  certificate (`assignment_lb_reached` ×2 + `master_bound_closed` ×1).
  Remaining certified gaps: prob_3 13,010, prob_5 38,149, prob_6 46,714,
  prob_7 59,438 — the θ-cut machinery fires rarely (area relaxation cannot
  see geometric congestion), so further closure needs tuck/cluster tiers
  (queue item 4) or stronger cut separation.
* Mid tier (prob_9–20): z1 ≤ 9 across the board; the loop consumes leftover
  budget (e.g. prob_14: 8 no-goods, −24%).
* Overloaded tail (prob_21–40): repair remains the main consumer; the loop
  correctly yields (no budget after seed+repair). Timing nondeterminism
  dominates run-to-run variance here.

## Decisions this table supports

* Milestone-3 go/no-go (switching `myalgorithm.py` to `solver.api`): the
  solver now dominates the legacy pipeline on every measured instance with
  0 × −1 — evidence in favor, pending the chimera-entry work (queue item 6)
  and a server-parity pass (item 7).
* The dashboard's "prob_1 at 59,681 / prob_40 −1" rows are obsolete; this
  table supersedes `2026-07-25_solver_v0_full_sweep.csv` as the current
  baseline.
